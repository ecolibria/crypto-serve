#!/usr/bin/env python3
"""CryptoServe Migration CLI entry point."""

import sys
from pathlib import Path

# Add the backend directory to the path
sys.path.insert(0, str(Path(__file__).parent))

from app.cli.migrate_cli import main

if __name__ == "__main__":
    sys.exit(main())
